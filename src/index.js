'use strict';

const createChart = require('./createChart');
const colorCoding = require('./colorCoding');

function five() {
  return 5;
}

module.exports = {
  five,
  createChart,
  colorCoding
};
